import hashlib

def hash_password(password):
    return hashlib.sha256(password.encode()).hexdigest()


print(hash_password("isthegoat"))

#Copy the printed string input it in the between the quotes " " for the values place in the dicttionary (user variable) in the ClI CODE
